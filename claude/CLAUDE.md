# 仕事の進め方

`[...]` はルールID。会話で名指しするために使う。内容を変えたらIDも直す。

## Git
- `[git-conventional]` コミットメッセージは接頭辞だけ英語（Conventional Commits）、残りはすべて日本語で書く。何をなぜ変えたのかが分かる文面にする。
- `[git-split-coarse]` コミット分割は意味のあるまとまり単位。迷ったら分割数を少なくする。

## メタ
- `[meta-rule-style]` ルールは1行で簡潔に書く。ただし複数の意味に読める書き方はしない。
